// import React from "react";
import Timeline from "../components/LineTime";

const Oferts = () => {
  return (
    <div>
      <Timeline activeStep={1} />
      {/* poner el ---- en otro componente */}
      <Timeline activeStep={1} />
    </div>
  );
};

export default Oferts;
